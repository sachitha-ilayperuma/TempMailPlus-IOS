# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside

# SwiftPM
.swiftpm/
# NOTE: Package.resolved is intentionally NOT ignored — for an app (not a library), it should
# be committed so everyone/CI builds against the exact same resolved dependency versions.

# macOS
.DS_Store

# Secrets (added in later phases)
GoogleService-Info.plist
